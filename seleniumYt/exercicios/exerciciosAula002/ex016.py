"""
faça um programa, com uma função, que calcula a média de
uma lista. Funções imbutidas que podem de ajusdar:
len(lista) calcula o tamanho da lista
sum(lista) faz o somatorio dos valores
"""

def media(lista_de_numeros):
    sum(lista_de_numeros) / len(lista_de_numeros)

print(media([1,2,3,4]))